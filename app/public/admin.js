/*document.addEventListener("DOMContentLoaded", function() {
    var mainContent = document.querySelector("main");
    if (mainContent) {
      mainContent.classList.add("contentToShow");
    }
  });*/

/*  $(window).on('resize', function() {
    var url = window.location.href;
    if(url.indexOf("?collapsed=true") > -1) {
        $('.sidebar-sticky.show ul').addClass('collapse navbar-collapse show');
    } else {
        $('.sidebar-sticky.show ul').removeClass('collapse navbar-collapse').addClass('show');
    }
}).trigger('resize');*/